# Gambas Form File 3.0

{ Form Form
  MoveScaled(0,0,121,78)
  Text = ("Chart View")
  { mnufile Menu
    Text = ("File")
    { mnusave Menu
      Text = ("Save to PNG")
    }
    { mnuexport Menu
      Text = ("HTML View")
    }
    { mnustatistics Menu
      Text = ("Data File")
    }
  }
  { Panel1 Panel
    MoveScaled(0,0,120,74)
    { Panel2 Panel
      MoveScaled(0,0,118,4)
      { ZoomSlider1 Slider
        MoveScaled(9,0.5,52,3)
        MinValue = 1
        MaxValue = 1000
        Value = 100
      }
      { btnleft Button
        MoveScaled(1,0.5,6,3)
        Picture = Picture["icon:/small/rotate-left"]
      }
      { btnright Button
        MoveScaled(62,0.5,6,3)
        Picture = Picture["icon:/small/rotate-right"]
      }
    }
    { ImageView1 ImageView
      MoveScaled(1,5,116,67)
      Expand = True
    }
  }
}
